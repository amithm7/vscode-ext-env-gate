# Env Gate

Warns when opening `.env` files, which may contain secrets, (excludes `.env.example` files).

## Features

![gate screen](./images/screen-gate-1.0.0.png)

- Use command `Env Gate: Reset Session` to clear session of remembered `.env` files, which bypass the gate.

## Release Notes

### 1.0.1

Initial public release

---

## Roadmap

- [ ] Allow users to edit the excluded list
